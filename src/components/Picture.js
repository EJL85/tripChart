import React from 'react';

class Picture extends React.Component {
    render() {
        return(
            <div className={`tripLeft ${this.props.showPicture}`}>
                <h1>This section currently under construction</h1>
                <h1>Picture</h1>
            </div>
        );
    }
}

export default Picture;